export enum ThirdNotificationPlatform {
  feishu = 'feishu',
}

export enum ThirdNotificationAccountStatus {
  normal = 'normal',
  disabled = 'disabled',
}

export enum ThirdNotificationChannelStatus {
  normal = 'normal',
  deleted = 'deleted',
}

export enum ThirdNotificationRecordStatus {
  pending = 'pending',
  success = 'success',
  error = 'error',
}
