export enum NotificationWebhookPlatform {
  Feishu = 'feishu',
  Slack = 'slack',
}

export enum NotificationWebhookRecordStatus {
  Pending = 'pending',
  Succeeded = 'succeeded',
  Failed = 'failed',
}
