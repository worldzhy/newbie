
{
  "config-service": {
    "cloudformation": {
      "token":{
        "secret": "process.env.AWS_CLOUDFORMATION_SECRETKEY_TOKEN_SECRET || 'your-secretkey-token-secret'"
      }
    }
  },
  "env":{
    "AWS_CLOUDFORMATION_SECRETKEY_TOKEN_SECRET":""
  },
  "dependencies": {
    "@aws-sdk/client-cloudformation": "^3.454.0",
    "@nestjs/jwt": "^10.2.0"
  }
}
